/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    "./src/**/*.{js,jsx,ts,tsx}" // Ensure all JS/JSX/TS/TSX files in `src` are scanned
  ],
  theme: {
    extend: {},
  },
  plugins: [],
};
